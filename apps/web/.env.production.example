# Copy to apps/web/.env.production and fill in your deployed worker URL.
# Used by Vite at build time. The same value can also be set as a GitHub
# Actions repo variable (Settings → Secrets and variables → Actions → Variables → New variable: VITE_API_BASE).
VITE_API_BASE=https://globe-watch-worker.YOUR-ACCOUNT.workers.dev
